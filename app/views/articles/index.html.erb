<div class="mt-5">
  <% @articles.each do |article| %>
  <article>
    <h2 >
      <%= link_to "#{article.title}", article_path(article.id), class: "article-header" %>
    </h2>
    <%= article.content %>
  </article>
  <% end %>
</div>
